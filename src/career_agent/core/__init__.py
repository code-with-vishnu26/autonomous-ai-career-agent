"""Core building blocks: event bus, plugin registry, config, and shared models.

This package holds the foundations every agent depends on. The plugin registry
and event bus (Phase 3) are what let new capabilities register without core
rewrites. Concrete interfaces land in Phase 2.
"""
