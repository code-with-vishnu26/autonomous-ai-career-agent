"""Human Review Center (Phase 52, ADR-0070).

The one place a prepared, unsubmitted `ApplicationSession` can transition
to `APPROVED`. Records human intent only -- never touches a browser, never
clicks anything, never submits anything.
"""
