# Architecture Decision Records (ADRs)

This directory records the significant architectural decisions made on this
project, and the context and consequences behind them. ADRs are immutable once
accepted: if a decision changes, we add a new ADR that supersedes the old one
rather than editing history.

[ADR-0000](0000-project-philosophy.md) is the **root**: it states the mission,
goals, non-goals, engineering principles, and golden rules. Every other ADR is
made in service of it; when two decisions conflict, the one more aligned with
ADR-0000 wins.

## Format

Each ADR is a numbered Markdown file: `NNNN-short-title.md`, using the template
below. Every ADR **must** end with *Future revisit criteria* so decisions stay
open to change instead of freezing forever.

```markdown
# ADR-NNNN: Title

- **Status:** Proposed | Accepted | Superseded by ADR-XXXX
- **Date:** YYYY-MM-DD
- **References:** (optional) related ADRs

## Context
What is the situation and the forces at play?

## Problem
The specific question this ADR answers.

## Decision
What we decided to do.

## Alternatives considered
What else we weighed and why we didn't choose it.

## Trade-offs
The costs we accept in exchange for the benefits.

## Consequences
The results — positive, negative, and neutral — of the decision.

## Future revisit criteria
The conditions under which we should reopen this decision.
```

## Index

| ADR | Title | Status |
|-----|-------|--------|
| [0000](0000-project-philosophy.md) | Project philosophy (mission, goals, non-goals, golden rules) | Accepted |
| [0001](0001-agent-oriented-architecture.md) | Agent-oriented architecture (not a fixed pipeline) | Accepted |
| [0002](0002-search-provider-abstraction.md) | Search provider abstraction (capabilities + health-based ranking) | Accepted |
| [0003](0003-truthfulness-gate.md) | Truthfulness gate (per-statement evidence, confidence, explainability) | Accepted |
| [0004](0004-plugin-architecture.md) | Plugin architecture (everything external is replaceable) | Accepted |
| [0005](0005-event-bus.md) | Event bus (loose coupling, no direct agent calls) | Accepted |
| [0006](0006-json-resume-master-profile.md) | JSON Resume master profile (single source of truth) | Accepted |
| [0007](0007-planner-agent.md) | Planner Agent (coordinator only; Decide as swappable step) | Accepted |
| [0008](0008-human-in-the-loop.md) | Human-in-the-loop application (pause, never bypass) | Accepted |
| [0009](0009-learning-engine.md) | Learning engine (improve from real outcomes) | Accepted |
| [0010](0010-hybrid-application-strategy.md) | Hybrid (tiered) application strategy (API → browser → email) | Accepted |
| [0011](0011-structured-tailored-content.md) | Structured tailored resume content (not free text) | Accepted |
| [0012](0012-opportunity-provenance-and-confidence.md) | Opportunity provenance + extraction confidence (honest uncertainty) | Accepted |
| [0013](0013-held-candidate-mechanism.md) | Held-candidate mechanism for freeform extraction sources (visible discard pile) | Accepted |
| [0014](0014-cross-source-opportunity-identity.md) | Cross-source opportunity identity: two-key dedup + canonical company | Accepted |
| [0015](0015-web-search-classification.md) | Web-search results are classified, not trusted (applies ADR-0013 to search) | Accepted |
| [0016](0016-truthfulness-gate-verification.md) | Truthfulness gate verification: entailment, categories, and the ClaimVerifier harness | Accepted |
| [0017](0017-master-profile-loader.md) | Master profile loader: required ids, scoped content hash, plain function not a Protocol | Accepted |
| [0018](0018-submission-safety.md) | Submission safety: structural approval (SubmittableApplication), confirmation-token binding, verifier isolation | Accepted |
| [0019](0019-ats-kind-resolution-and-tier-fallback.md) | ATS-kind resolution (reused ADR-0015 pattern-match, no new repository) and no cross-tier auto-retry | Accepted |
| [0020](0020-browser-tier-session-and-pause.md) | Browser-tier session encryption (OS-keychain-backed, fail-closed) and the token-bound pause/resume guarantee, proven against real Playwright | Accepted |
| [0021](0021-email-tier-draft-only.md) | Email tier is draft-only: EmailDraftSink has no send method, submit() never claims ApplicationSubmitted | Accepted |
| [0022](0022-resume-generator.md) | ResumeGenerator: structural summary (no drafter write access), narrow ContentDrafter port not permanently cascade-exempt, no self-verification | Accepted |
| [0023](0023-resume-tailoring-pipeline.md) | Resume-tailoring pipeline: composition stops at SubmittableApplication (no Applicator call), new "rejected" status distinct from "failed", reuses dormant ResumeTailored/TruthfulnessRejected events | Accepted |
| [0024](0024-real-confirmation-and-submission-wiring.md) | Real human confirmation (cli.confirm_submission, no default-to-yes) and single-tier SubmissionPipeline; multi-tier selection confirmed unbuilt, deferred | Accepted |
| [0025](0025-resume-renderer.md) | Resume renderer: computed once at pipeline resume-creation time (no Applicator changes needed), raises loudly rather than silently dropping an unresolvable entry | Accepted |
| [0026](0026-real-apply-command-and-promptfoo-enforcement.md) | Real `career-agent apply` command: `--opportunity-file` handoff, promptfoo pass positively verified against a results artifact (not a claimed flag) before AnthropicClaimVerifier is constructed, stops at confirmation (no real ATSAdapter yet) | Accepted |
| [0027](0027-applicant-identity-snapshot.md) | Applicant identity snapshot: Application.applicant (required, frozen BasicsSection) prevents prepare/submit identity drift; BrowserApplicator._fill_form now uses real data with a documented, known-imprecise name split; Tier 1 direct-API submission confirmed dead across Greenhouse/Lever/Ashby | Accepted |
| [0028](0028-browser-tier-dispatch-and-unsupported-field-refusal.md) | Browser-tier per-ATS FormFiller dispatch (resolve_ats_kind, Lever/Ashby stubbed pending human verification); live-DOM-verified refusal of any required field no filler knows how to fill; custom-questions/EEOC answering deferred to its own ADR, with an absolute no-guess-no-confirm rule stated now for EEOC fields specifically | Accepted |
| [0029](0029-per-filler-challenge-and-submit-selectors.md) | Per-FormFiller challenge_selector/submit_selector (real hCaptcha markup found on a live Lever posting would never have matched Greenhouse-hardcoded literals) and name-based field-selector matching (real Lever fields have no id attribute); LeverFormFiller stays a stub since the resume field's real interaction shape is still unconfirmed | Accepted |
| [0030](0030-greenhouse-coverage-is-narrow.md) | Greenhouse's resume_text DOM-confirmed as a real "Enter manually" option (exact toggle interaction still unconfirmed); corrects the record that an ordinary real Greenhouse posting requires far more than identity+resume (Education, legal-status, full Voluntary Self-ID, Veteran Status), so "Tier 2 works" means narrow completion + correct refusal on most postings, not broad completion -- re-prioritizes the deferred custom-questions/EEOC design pass accordingly | Accepted |
| [0031](0031-question-answerer.md) | QuestionAnswerer: four custom-question categories (EEOC absolute with no MasterProfile access at all, LegalStatusSection-backed factual yes/no with None-means-uncaptured, always-human-authored subjective, deterministic no-guess dropdown matching with its own DropdownMatchResult type); deterministic template matching over an LLM call for all categories; user-authored 20-case adversarial matrix, four load-bearing cases independently verified by injection; DOM-wiring into BrowserApplicator deliberately deferred | Accepted |
| [0032](0032-question-answerer-wiring.md) | Wires QuestionAnswerer into BrowserApplicator's live pause/resume flow: two sequential pause phases (pre-click fields-need-human-input, post-click challenge), the human fills every manifested field directly on the visible page rather than through a typed answer payload (EEOC data never becomes a value this process holds), Application.legal_status extends the applicant frozen-snapshot precedent one field wider with zero new MasterProfile-storage dependency, _PausedSession's reason discriminator proven load-bearing by injection | Accepted |
| [0033](0033-resume-file-generation.md) | Resume file generation: deterministic ATS-safe DOCX (zip-timestamp-normalized; raw python-docx is not cross-second deterministic, verified) + LibreOffice-headless PDF as a derived non-reproducible view with a typed runtime-availability refusal; Education sourced read-only from MasterProfile, structurally absent from every generated type (authoritative Option (a)); ResumeArtifact content-hash-addressed filenames make silent overwrite impossible by construction; injection pass found and fixed the check-before-content gap and a 2s-ZIP-granularity test weakness | Accepted |
| [0034](0034-ats-score-gate.md) | ATS score gate: deterministic curated-taxonomy scoring is the entire pass/fail authority (spaCy model rejected -- artifact-dependent determinism is not determinism); passed computed in the report type itself (threshold + hard-format override); advisory semantic layer prunes the gap report only, every claim verbatim-verified, deliberately NOT cost-cascade-exempt (gates nothing, reasoning recorded); retailor loop with truthfulness-gate-before-every-score, GENUINE gaps structurally unreachable by the drafter (AtsGapReport has no field for them), convergence detection, trajectory-carrying typed refusal; reviewer-drafted 14-case matrix, four load-bearing cases injection-verified | Accepted |
| [0035](0035-real-lever-form-filler.md) | Real LeverFormFiller from ADR-0029's recorded live-DOM evidence: single unsplit full-name field, required file upload satisfied by attaching the application's own ADR-0033 DOCX artifact via set_input_files (FileList-verified, injection-proven against a wrong-file swap), typed MissingResumeArtifactError precondition refusal, hCaptcha through ADR-0020's pause/resume unchanged -- live validation on the user's machine stays the named final check | Accepted |
| [0036](0036-worldwide-job-board-sources.md) | Eight Tier A worldwide job-board sources (Adzuna/Reed/USAJobs/Arbeitnow/TheMuse/Remotive/RemoteOK/Jooble) behind the unchanged OpportunitySource Protocol with shared normalization, required provenance, RemoteOK attribution carried structurally, Jooble key never recorded (injection-verified); HttpClient.get_json gains additive headers; Tier B (JSearch) evaluated-not-built; Tier C (Naukri/Foundit/LinkedIn/Indeed/Seek) recorded manual-only -- no permitted programmatic path, no scrapers ever | Accepted |
| [0037](0037-persistence-discover-and-first-profile-writer.md) | SQLite persistence (fidelity-proven drop-in OpportunityRepository incl. close/reopen round-trip; append-only application audit trail), real discover command producing the exact ADR-0026 handoff files with per-source failure isolation, the first MasterProfile writer (yes/no/skip capture -- unrecognized input can never become an answer, injection-verified; unmodeled sections byte-identical), and the founding-brief Excel tracker export | Accepted |
| [0038](0038-decide-layer.md) | Decide layer: deterministic weighted ranking inside the Planner boundary (profile match via Phase 10's unforked keyword machinery 50%, source reliability 20%, freshness 20%, salary-transparency bonus 10%); config filters are hard excludes with named reasons (injection-verified against penalty-conversion), excluded opportunities returned visibly; zero LLM calls in v1; salary floor deliberately absent (no structured field -- named, not silent) | Accepted |
| [0039](0039-learn-pillar.md) | Learn pillar: typed outcome recording attached only to real recorded applications (append-only history), per-variant funnels keyed to prompt/profile/ATS band reading the FULL outcome history with rejection stages separated; raw counts only at personal N -- no significance testing, no bandit routing, mandatory small-sample caveat (injection-verified) and tested absence of prescriptive verdicts | Accepted |
| [0040](0040-notifications-and-dashboard.md) | Notifications: Telegram (token never logged/stored/in-errors, tested) + ntfy zero-setup fallback through the existing HttpClient port; NotifyingSubscriber is notify-never-gate (injection-verified). Local read-only Streamlit dashboard with pure, tested metric preparation and the ADR-0039 caveat intact; reads SQLite as a separate read model so the repository contract stays add/get | Accepted |
| [0041](0041-scheduling-gates-and-bounded-auto.md) | Both pre-scheduling gates close: StaleProfileError refuses before prepare() ever runs on a profile-version mismatch (injection-verified, calls==[] proof), and SentMailChecker/confirm_email_sent advance email drafts only on positive SENT observation with couldn't-check as a typed unknown (real OAuth Gmail impl stays user-validated); career-agent auto is a bounded cron-invokable pass that structurally cannot confirm or submit (co_names-level proof) -- submission stays human-gated forever | Accepted |
| [0042](0042-groq-free-tier-provider-for-non-exempt-llm-ports.md) | Groq (free tier, contractually not used for training) added as the preferred provider for ContentDrafter and SemanticKeywordMatcher only -- the two ports ADR-0022/ADR-0034 already ruled cost-cascade-non-exempt; ClaimVerifier stays Anthropic-only with no selection branch at all; provider chosen once at composition time, never per-call, so a Groq failure can never silently reroute to a paid call; no promptfoo re-run required (promptfoo scopes ClaimVerifier only, unchanged) | Accepted, partially superseded by 0043 |
| [0043](0043-zero-cost-truthfulness-gate-provider.md) | On explicit user override of ADR-0016's exemption (asked directly, given the stated risk, before any code changed): GroqClaimVerifier (openai/gpt-oss-120b, Groq's strongest free reasoning model) added as a second real ClaimVerifier, preferred by select_claim_verifier when GROQ_API_KEY is set; the compensating structural control is promptfoo_gate.py becoming provider-keyed (`{prompt_version}--{provider_id}.json`), closing the gap where an Anthropic pass could otherwise silently authorize an unvalidated Groq verifier; live promptfoo validation against gpt-oss-120b remains the named, outstanding, user-run requirement before real use | Accepted |
| [0044](0044-formal-claim-evidence-entailment-and-deterministic-precheck.md) | Formal, bounded claim-evidence entailment: a new deterministic Layer-1 precheck (technology/metric/verb-strength/seniority, reusing the ATS gate's curated taxonomy, no NLP model) resolves clear-cut claims before the LLM is ever called; individually audited (not mechanically flipped) the 4 disputed matrix cases -- #1/#9/#11 flip to BLOCK (skill noun ≠ action, weaker verb ≠ stronger verb), #8 stays APPROVE on its own merits as a genuine safe abstraction; prompt bumped to truthfulness-gate-v2 (2 new categories), which ADR-0043's provider+version-keyed results filenames automatically invalidate all prior v1 passes for, no new code required | Accepted |
| [0045](0045-pareto-dominance-and-weight-sensitivity-analysis.md) | Bounded first slice of Research-track R4 (repo audit found R4/R5/R6/R8's own trigger criteria all unmet -- no observed weighted-sum failure, no budget signal, N too small): `domain/pareto.py` (generic nominal + confidence-interval-robust Pareto dominance/frontier over Decide's four existing 0-100 objectives, decoupled from `DecisionScore` per the domain-purity import-linter contract) + `agents/planner/sensitivity.py` (closed-form, two-point-linear-solve weight-flip analysis for adjacent ranked pairs, empirically verified by reconstruction). Purely additive -- `DeterministicDecideScorer.rank()` unchanged, not wired into any CLI output this slice; portfolio selection, full-simplex Monte Carlo, and calibrated uncertainty all named as still-unjustified future work | Accepted |
| [0046](0046-decision-intelligence-advisory-integration.md) | Wires ADR-0045's Pareto/sensitivity analysis into `discover`'s existing ranked-summary output as read-only advisory annotations only -- `DeterministicDecideScorer.rank()` and `auto`'s scalar-order selection are structurally untouched (`co_names`-verified), dominance computed over the full included set (never a display-truncated slice), sensitivity bounded to the #1-vs-#2 pair only, evidence-quality band explicitly labeled a heuristic (never a calibrated probability); closes a Phase 19 test-coverage gap (confidence=0.0, equal-confidence, monotonicity-under-improved-evidence-quality), confirming the original code was already correct | Accepted |
| [0047](0047-decision-intelligence-validation-and-benchmark-methodology.md) | Validates (not extends) ADR-0045/0046 against a repository audit: proves -- algebraically and by an exhaustive 390,625-pair finite-grid search (`research/decision_benchmarks.py`, zero counterexamples) -- that under the current architecture the scalar-ranking winner can never be Pareto-dominated (all four weights strictly positive, same objective correspondence). Closes further Phase 19/20 test gaps (duplicate objective vectors, direct just-below/above-breakeven recomputation, hard-feasibility isolation from a hypothetically dominating excluded candidate, submission-path `co_names` proof). Evidence gate concludes no further algorithm (portfolio, Bayesian, Monte Carlo, bandits) is justified yet | Accepted |
| [0048](0048-application-attempt-idempotency-guard.md) | Phase 22 production-reliability audit found no cross-run protection against re-applying to the same opportunity (`Application.id` is a fresh UUID per run; opportunity-level dedup (ADR-0014) never covers the application-attempt layer): adds `SqliteApplicationStore.prior_attempt_status()` and wires a refuse-outright guard into both `apply` and `auto`, skipping only `"rejected"` (no-side-effect) priors; explicitly does NOT add a state machine, automatic retry/backoff, a checkpoint journal, a `SUBMISSION_UNCERTAIN` state, or a new observability framework -- none justified by anything actually observed in this repository at its real (single-user, tens-of-applications) scale | Accepted |
| [0049](0049-append-only-execution-journal.md) | Phase 23 audit found the one thing its brief centrally worried about -- an irreversible external submission -- is structurally unreachable from any composition-root command (`TieredApplicator`/`BrowserApplicator`/`EmailApplicator`/`SubmissionPipeline` are never imported by `cli.py`, confirmed by grep), which reshapes what's justified: adds a minimal, append-only `SqliteRunJournal` (`domain/journal.py` + `storage/sqlite.py`) giving `apply`/`auto` a stable per-invocation `run_id` and reconstructable stage history for auditability/crash-forensics, composed with (not replacing) ADR-0048's guard; explicitly does NOT add a validated transition-gated state machine, a recovery planner, or a `SUBMISSION_UNCERTAIN` state, since nothing reachable today is unsafe to simply re-run -- the exact trigger for building those (wiring any real `Applicator` into `cli.py`) is named and deferred, not built speculatively | Accepted |
| [0050](0050-execution-safety-boundary.md) | Phase 24 builds the exact prerequisite ADR-0049 deferred: a pure, deterministic, fail-closed execution-permission boundary (`domain/execution.py`) with an explicit four-way `SubmissionOutcome` (incl. `OUTCOME_UNCERTAIN`, never collapsed into failure), a retry-admissibility rule where `OUTCOME_UNCERTAIN`/`DEFINITELY_SUBMITTED` are never auto-retryable, deterministic source-policy resolution (no source maps to `AUTOMATED` -- ADR-0027 killed every fully-automated path), and a reference confirmed-artifact digest. The whole 256-point input space is exhaustively enumerated (`research/execution_safety.py`) with zero invariant/metamorphic counterexamples. Wired live-but-behavior-preserving into `apply` after confirmation with `executor_available=False` (no executor is safe to wire: browser=no-exception-means-success, email=draft-only, direct-API=dead), so it always refuses with an explicit journaled reason -- **no executor wired, no external submission newly reachable**; composes ADR-0048 (finer prior-outcome typing) and ADR-0049 (emits `EXECUTION_REFUSED`); write-ahead intent + a real ack classifier are specified and deferred to the eventual executor-wiring phase | Accepted |
| [0051](0051-guided-setup-onboarding-command.md) | Phase 25 cross-repo audit (vs. MIT-licensed `MadsLorentzen/ai-job-search` @ `79b1537`) found our architecture stronger on rigor/safety but weaker on time-to-first-result: we had no onboarding command, forcing users to hand-author a JSON Resume profile from the pydantic schema. Adds a deterministic, fully-offline `career-agent setup` (`run_setup_command` + `example_profile_dict`/`write_profile_scaffold`): scaffolds a schema-correct starter profile iff none exists (never overwrites a real one), prints an offline readiness report (profile loads? provider key present -- value never printed? Promptfoo artifact present?), and names the single next command. Zero LLM/network/cost; touches no safety gate. LLM CV-extraction and a truthfulness-re-gated drafter/reviewer loop were evaluated and deferred (the reference's reviewer loop can introduce unsupported claims with only a prompt instruction as its guard) | Accepted |
| [0052](0052-evidence-grounded-cv-ingestion.md) | Phase 26 adds CV ingestion without letting a parser/regex/inference silently create trusted facts. A pure `domain/ingestion.py` trust layer (`TrustState` = UNVERIFIED/CONFIRMED/REJECTED -- no VERIFIED state, since a promoted fact *becomes* a MasterProfile field; conflict is a derived property, not a state; **no confidence modeled**) keeps imported facts entirely outside `MasterProfile` until a fail-closed `promote()`: ADD only if CONFIRMED ∧ a content-bound confirmation (digest over id∥field∥value∥source∥evidence -- so value/source/replay drift all fail) ∧ evidence re-validates against the re-read source ∧ no unresolved conflict ∧ no different existing value (else REQUIRES_RESOLUTION, never a silent overwrite). Whole 108-point decision space exhaustively enumerated (`research/ingestion_promotion.py`), zero counterexamples. Two-step file CLI (`import-cv` → UNVERIFIED draft, never touches profile; `promote-cv` → source-drift + evidence re-checked). DOCX (declared `python-docx`) + TXT only; **no PDF/OCR/LLM, zero new dependency**; injection text in a CV is inert data (no LLM call); MasterProfile/gate/prompt-version/Promptfoo unchanged; no external submission newly reachable | Accepted |
| [0053](0053-truthfulness-regated-revision-loop-authority.md) | Phase 27 audit found the truthfulness-re-gated revision loop **already exists and is used** (ADR-0034's `_ats_gate_loop`: retailor up to 2×, FULL truthfulness gate before any ATS re-scoring, convergence detection, fail-closed on exhaustion, SURFACEABLE-only gap report). **Decision: Option A** -- add no LLM reviewer/reviser subsystem (a duplicate that would add a prompt-injection surface + LLM cost for marginal value, since any reviser output is re-gated anyway). Formalizes the authority model (truthfulness rejection is absolute and non-overridable; ATS/"reviewer" advice is SURFACEABLE-only and advisory; skills are structural) and adds the previously-unasserted *composition* invariant tests: the gate never receives the JD (`verify(draft, profile)` -- JD structurally can't be evidence, I9), no verification cache (every claim re-judged every call, I3), `agents/resume` never imports Phase 26 ingestion (I10/I11), and an end-to-end injection JD that makes the drafter add an unsupported skill yields a rejected application. No production code, no prompt-version bump, no Promptfoo change, no new dependency; no external submission newly reachable | Accepted |
| [0054](0054-production-readiness-release-gate.md) | Phase 28 production-readiness/release gate. **Decision: Option A** (tests + this ADR, no production code) -- the architecture already satisfies the requirement, so a `doctor` command was rejected as a duplicate of `setup`'s readiness report. Documents the reconstructed end-to-end state machine (S23 external submission and S25/S26 outcome-unknown/recovery are **unreachable** -- no executor is wired), a traced capability matrix, a fail-closed failure matrix, and the I1-I22 release-invariant contract with where each is proven. Adds a composed end-to-end dry-run (`test_phase28_end_to_end.py`: setup → import-cv → promote-cv → auto → prepared, + restart-idempotency + UTF-8 survival, synthetic Aarav-Rao fixture generated in-test) and the cross-cutting contract (`test_phase28_release_invariants.py`: no Applicator/submit reachable from the CLI, missing/malformed Promptfoo blocks the gate offline, apply/auto gate before constructing the verifier, network guard blocks real Groq/Anthropic but allows MockTransport, journal has no external-submission-success event). Core finding: release-ready as a supervised **prepare-only** product; **no irreversible external action exists today**. No dependency, no prompt/Promptfoo change, no safety semantics changed | Accepted |
| [0055](0055-bounded-real-provider-release-policy.md) | Phase 29 bounded real-provider release policy. Audited the real LLM path fresh: ClaimVerifier=`openai/gpt-oss-120b` (Promptfoo-gated) / `claude-opus-4-8`; ContentDrafter=`llama-3.3-70b-versatile`/`claude-opus-4-8`; SemanticMatcher=`llama-3.3-70b-versatile`/`claude-haiku-4-5-20251001`; Groq-preferred selection. One prepare run's LLM calls are structurally bounded (`_MAX_ATS_RETRIES=2`; Layer-1 precheck resolves clear claims with zero calls; reasoning capped at `max_tokens=2000`); absolute monetary cost UNKNOWN (not asserted). **This environment has no key/artifact, so a live run is BLOCKED_BY_CONFIGURATION -- not performed, not faked.** **Decision: Option A** -- no smoke harness (`apply` already is the bounded path); adds drift-guard tests (`test_phase29_provider_release_gate.py`: exact model-id pins, token/retry bounds, empty/whitespace-key edges, verifier-only Promptfoo scope) and documents the user-run controlled-smoke procedure, the Promptfoo-vs-Phase-29 distinction, and model/provider/prompt evidence-invalidation triggers. **Release decision: RELEASE_READY_WITH_LIMITATIONS** -- safety proven offline; real-output quality + live integration require the user's local smoke run. No production code, no dependency, no prompt/Promptfoo change, no live/paid call | Accepted |
| [0056](0056-v1-prepare-only-release-scope.md) | Phase 34 final v1.0 release audit. Fresh reality audit (the brief's "Phase 33/Phase 31/ADR-0059" do not exist; real state is Phase 30 merge `46d267a`, highest ADR 0055, baseline **667 passed/0 skipped/0 failed**, ruff clean, imports 4/4, clean-install + `--help` + `setup` smoke pass). Re-proved the safety architecture intact; external submission classified **UNREACHABLE** (no `Applicator` constructed, `executor_available=False` fail-closed). One release-blocking gap found: the README **overclaimed** (said "submit through a tiered applicator", "scaffolding only / not yet runnable", Anthropic-only cascade). **Decision: Option B** (docs + release artifacts + drift-guard tests, no production code). Formalizes three durable decisions: **(1)** v1.0 scope = `PREPARE_ONLY`; **(2)** release-candidate policy → version `0.1.0 → 1.0.0rc1`; **(3)** supported-platform policy (Linux exercised; Windows UTF-8 static-only; macOS untested). Adds `SECURITY.md`, `RELEASE_CHECKLIST.md`, `docs/release/v1.0.0-rc1-notes.md` (capability matrix + limitations), `test_phase34_release_audit.py`. **Release decision: CONDITIONAL_GO** -- no critical invariant violated; conditions are live-output quality unvalidated, live Promptfoo BLOCKED_BY_CONFIGURATION, no in-repo CI, Windows/macOS execution untested. No safety semantics changed; no external submission newly reachable; no live/paid call | Accepted |
| [0057](0057-ci-and-cross-platform-release-hardening.md) | Phase 35 CI and cross-platform release hardening. Fresh audit confirmed the one gap Phase 34 named: no `.github/` directory, no CI. Adds `.github/workflows/ci.yml` (matrix `ubuntu-latest`x`windows-latest`, Python 3.11; `permissions: contents: read`; no secret ever referenced -- structurally cannot make a live/paid LLM call; no `continue-on-error`; steps: ruff, lint-imports, pytest, `python -m build`, artifact-content check, clean-venv install + CLI smoke). Adds `scripts/verify_release_artifacts.py` (checks wheel **and** sdist; fixes two false positives found while building it: `.env.example` is a safe template not a real `.env`, and `tests/` belongs in the sdist by normal packaging convention -- only forbidden in the wheel) and `scripts/smoke_test_wheel.py` (one OS-branch-free script, not duplicated YAML, proving install+`--help`+`setup` on both OSes identically). Amends ADR-0056's platform table from *static reasoning* to *evidence*: Windows is now actually exercised in CI on every push/PR; macOS remains a named, deliberate gap (10x runner-cost multiplier, no evidence of a macOS-specific defect). `tests/test_phase35_ci_release_tooling.py` (6 tests) pins the scripts' pure logic and the workflow's fail-closed shape. No production code, no dependency beyond declaring `build>=1.2` (previously installed ad hoc), no safety-semantics change, no external submission newly reachable | Accepted |
| [0058](0058-confirmation-lifecycle-and-retry-semantics.md) | Phase 36 controlled live Groq validation on the user's real Windows machine found a real defect: the first genuinely live-Groq `apply` run tailored, gated, ATS-scored (`78.125`), and rendered successfully, but declining the confirmation prompt (`N`) left the application-store row stuck at `status="pending"` (recorded unconditionally *before* confirmation, never updated after), permanently refusing a retry with a message wrongly asserting real-world-submission risk. Reproduced offline first, no live call (`test_declined_confirmation_does_not_permanently_block_retry`, fails pre-fix). **Decision: Option F** (minimal ordering + status fix, no state machine, no migration) -- `Application.status` gains `"declined"` (means the same as `"rejected"`: zero external side effect); `_apply_pipeline` now records once per run, at the correct terminal branch (rejected/declined/pending), instead of once unconditionally before confirmation; `prior_attempt_status()` excludes `"declined"` alongside `"rejected"`. Genuinely risky statuses (`pending`/`paused_for_human`/`submitted`/`failed`) remain fully blocking, proven by a dedicated test. External submission remains **UNREACHABLE**; no truthfulness/ATS/Promptfoo/journal/prompt-version change; no schema migration (no `CHECK` constraint existed); 4 new tests (2 CLI-level, 2 storage-level) | Accepted |
| [0059](0059-v1-0-0-release-promotion.md) | Phase 37 v1.0.0 release promotion decision. Fresh audit confirmed `origin/main` at `833c5db` (real PR #58 merge, verified via `git log`) and, critically, independently checked the GitHub Actions API directly (not inferred from YAML) -- both `verify (ubuntu-latest)` and `verify (windows-latest)` on that exact commit are `completed`/`success`, every step green, resolving Phase 36's unconfirmed-CI gap with a real result. Local baseline reconfirmed: **682 passed/0 skipped/0 failed**, ruff clean, imports 4/4 -- matching CI's own count. Re-inspected the maintainer's real Phase 36 live-Groq transcript directly: the adversarial injection's targets ("8 years of Kubernetes", "led 20 engineers", any "Senior" framing) do **not** appear anywhere in the real rendered resume text; ATS `78.125` and `truthfulness_approved=1` are real SQLite values, not asserted from policy. **Decision: GO** -- all four of ADR-0056's `CONDITIONAL_GO` conditions are now closed by real evidence (live-output quality, live Promptfoo PASS, in-repo CI now green, Windows now exercised); macOS remains a named, deliberate, cost-driven gap, not a blocker. Promotes `pyproject.toml` version `1.0.0rc1 → 1.0.0`; updates the version-pin test; adds `docs/release/v1.0.0-notes.md` (condition-closure table + real live-evidence summary) while keeping the historical rc1 notes file unedited with a superseded-by pointer; adds `tests/test_phase37_v1_release_promotion.py`. No production code changed; no safety semantics changed; no new dependency; no prompt-version/Promptfoo change; **no git tag created, nothing published** -- both explicitly left for the maintainer | Accepted |
| [0060](0060-promptfoo-path-portability.md) | Phase 40 fixed the v1.1-backlog P1: the Promptfoo results-directory default (`_REPO_ROOT`/`__file__`-based) only resolved correctly for an editable install -- a wheel or non-editable `pip install .` put it inside `site-packages`. Reproduced live: a fresh wheel install, run from outside the repo, reported the broken path before the fix. **Decision:** fix by consistency, not new design -- `Settings` gains `promptfoo_results_dir` (CWD-relative, `.env`-overridable), mirroring `database_path`/`artifacts_dir` exactly; `_REPO_ROOT`/`_DEFAULT_PROMPTFOO_RESULTS_DIR` deleted; all five call sites (`setup`/`apply`/`auto`/`verify-promptfoo`/`diagnose-promptfoo-drift`) now resolve from `settings.promptfoo_results_dir`. Durable policy recorded (not merely a bugfix): no future runtime path may be `__file__`/install-tree-relative. 6 new tests; re-verified live on both an editable install and a fresh wheel install from outside the repo. No Promptfoo gate semantics changed, no new dependency | Accepted |
| [0061](0061-sdist-allowlist-and-classifier-accuracy.md) | Phase 41 installed-package/distribution audit built a fresh sdist and found a real leak: `.claude/` (this agent's own local session state) and `.import_linter_cache/` -- both untracked but not `.gitignore`d -- appeared as top-level sdist entries, since hatchling's default sdist packaging includes anything not explicitly `.gitignore`d. Confirmed hatchling does respect `.gitignore` (a `*.db`-matching control file was correctly excluded); the gap was `.gitignore`'s coverage, not the mechanism. **Decision:** `.gitignore` gains both entries; `scripts/verify_release_artifacts.py` gains a **positive top-level allowlist** for the sdist (`_SDIST_ALLOWED_TOP_LEVEL`) -- a durable policy, since a suffix/fragment blocklist can never catch an unanticipated directory; verified both ways (reintroduced junk fails, current sdist passes). Also fixed two stale metadata facts found in the same audit: `pyproject.toml`'s classifier (`2 - Pre-Alpha` -> `5 - Production/Stable`, matching the actual tagged v1.0.0 state) and `requirements.txt`'s comment (abandoned "Haiku->Sonnet->Opus cascade" -> actual Groq-preferred/Anthropic-fallback policy). Confirmed live: fresh wheel **and** sdist installs, each in an independent clean venv, run from outside the repo -- `--help`/`setup`/`verify-promptfoo`/import/metadata all correct, distribution name (`career-agent`) distinct from repo name and import package name (`career_agent`). 5 new tests. No safety semantics changed, no dependency version changed, no new CI platform (wheel-install already exercised on both Ubuntu and Windows, confirmed from existing green CI, not assumed) | Accepted |
| [0062](0062-v1-1-readiness-audit-and-version-decision.md) | Phase 43 audited readiness to cut **v1.1** across eight categories and decided the version by real SemVer reasoning, not by assuming the "v1.1" label. Enumerated every commit since the `v1.0.0` tag (Phases 38–42): the **only** runtime `src/` change is a new `promptfoo_results_dir` `Settings` field (+ the behavior-preserving cli refactor that uses it); everything else is docs/tests/packaging-metadata/CI. No LLM-facing code changed (gate, verifiers, prompt version `truthfulness-gate-v2` all byte-identical to v1.0.0). Behavior is unchanged for supported usage (editable install from repo root), a bugfix for the previously-broken wheel case. **Decision:** target **v1.1.0** (MINOR — a new backward-compatible config field is functionality, so not v1.0.1; no blocker, so not "unreleased"); release gate **CONDITIONAL_GO**, conditioned on Phase 44 mechanics (bump `pyproject` 1.0.0→1.1.0 in lockstep with the three v1.0.0 drift-guard tests — updated, never weakened — plus v1.1.0 release notes and fresh green CI) and owner authorization for the tag. No fresh live-LLM run required since no LLM-facing code changed. No P0/P1 found; `v1.0.0` tag immutable. This ADR records the decision only — it bumps no version and creates no tag | Accepted |
| [0063](0063-github-release-artifact-distribution-policy.md) | Phase 45 post-release reconciliation, after the maintainer manually pushed the annotated `v1.1.0` tag and published a GitHub Release. Verified integrity: `v1.1.0` tag object `0815…` peels to `a563dbe…` (= `origin/main` = Phase 44 merge = owner evidence); GitHub Release confirmed via API (id `352138767`, `draft=false`, `prerelease=false`, `published_at=2026-07-10T14:14:01Z`); `v1.0.0` untouched (`0ddcda04…`/`b8414e3…`). The Release had **no assets**. **Decision:** `KEEP_SOURCE_ARCHIVES_ONLY` — GitHub Releases publish source archives only; no binary wheel/sdist assets are attached, matching the README's already-test-pinned "editable-install-only, no published asset" policy and avoiding an unprovable build-provenance claim (a fresh `career_agent-1.1.0` wheel/sdist was built and verified as evidence, but intentionally not uploaded). Also reconciled stale pre-release wording in `docs/release/v1.1.0-notes.md` and the README Status, drawing an explicit line between **software release state = `RELEASED`** and **product posture = `PREPARE_ONLY`**. No version bump, no safety change, no tag mutation; history not rewritten (dated reconciliation section instead) | Accepted |
| [0064](0064-job-search-preferences-separate-from-profile.md) | Phase 46 begins a v2 automation-layer arc on top of the existing prepare-only engine, starting with a Job Search Preferences model (titles, seniority, location, salary, company allow/deny lists, plus deferred behavior toggles). **Decision:** a wholly separate model/file (`domain/job_preferences.py`, `storage/job_preferences.py`, `job_preferences.json`, `Settings.job_preferences_path`) and CLI command (`career-agent preferences`), never merged into `MasterProfile`/`profile.json` — preferences are search/behavior configuration, not applicant-facing facts the truthfulness gate grounds against, and merging them would make `profile_version`'s content hash chase casual search-preference edits that have nothing to do with what's true about the candidate. No JSON-Resume-style camelCase mapping layer needed (this schema is this project's own design). Only `preferred_titles`/`alternative_titles`/`work_mode`/`countries`/`keywords_exclude` are wired this phase, via a pure `generate_search_queries` function that `build_discovery_sources` fans keyword-capable sources (Adzuna/Reed/USAJobs/Jooble) out across — proven byte-identical to prior behavior when no preferences are configured, not just asserted. Every other field is captured but explicitly documented as **not yet enforced** (salary/visa/company lists, `require_human_confirmation` — informational only, cannot bypass the real hardcoded confirmation boundary — auto-tailor/auto-cover-letter toggles, `max_applications_per_day`). Incidental fix in the same change: `profile.json` (the actual default filename every command uses) was never gitignored at all — only the unused `master_profile.json` pattern was; both now correctly ignored. No safety-boundary change, no LLM-facing change, no browser automation (foundation only; that begins Phase 47) | Accepted |
| [0065](0065-browser-automation-foundation.md) | Phase 47, second phase of the v2 automation-layer arc: a Browser Automation Foundation (launch Chrome, reuse/persist sessions, detect login, never automate login, multi-tab support) that explicitly "does NOT yet apply for jobs." The repository-reality audit found this is not greenfield: `agents/apply/browser_applicator.py` (ADR-0020/0028/0032) already drives real Chromium with form-filling/submit-click, unwired from the CLI, and `integrations/browser_session.py`'s `EncryptedSessionStore` (ADR-0020) already provides encrypted-at-rest session persistence. **Decision:** extract and share, don't duplicate — new `BrowserManager`/`SessionManager`/`TabManager` under `integrations/browser/` (not a new top-level `browser/` package: `storage`/`integrations` are this project's existing unlayered I/O branches, and a live Chromium instance is unambiguously "an integration," the same category as `browser_session.py`/`http.py`). `SessionManager` reuses `EncryptedSessionStore` unchanged and adds the one genuinely new primitive, `wait_for_login` — structurally cannot type a credential (verified by an AST-based test scanning for any `.fill()`/`.type()`/`.press()` call, not a text search that a docstring mentioning those method names would falsely trip). A fifth import-linter contract + AST-based purity test enforce that `integrations.browser` has zero knowledge of jobs/résumés/applications, mirroring `domain/`'s existing zero-I/O enforcement pattern. 28 new tests, all driven against a real local Chromium instance (same sandbox-path discovery as `test_browser_applicator.py`), not mocks. No CLI command, no change to `BrowserApplicator`/the execution-safety boundary/`executor_available=False`, no website-specific logic (Phase 48), no automated login of any kind. No new dependency (`playwright` already required), no version bump | Accepted |
| [0066](0066-website-adapter-framework.md) | Phase 48, third phase of the v2 arc: a Website Adapter Framework (common interface over Greenhouse/Lever/Ashby/Workday/RemoteOK/Remotive/Arbeitnow/TheMuse). The audit found six of seven providers already have a real, working, tested, API-based `OpportunitySource`; `resolve_ats_kind` already does deterministic provider detection; `FormFiller` already establishes verified-vs-stubbed capability discipline (Greenhouse/Lever real, Ashby an honest stub). Only Workday has zero prior art. **Decision:** `search()` delegates to each existing source rather than re-scraping through a browser; no new canonical job model (reuses `Opportunity`, capability flags live on the adapter class instead, mirroring `ProviderCapabilities`'s existing "declared on the interface" pattern); capabilities are grounded in `FormFiller`'s real evidence (Greenhouse resume field is verified text not upload → `supports_resume_upload=False`; Lever's is a verified required file upload → `True`; everything else unverified → `False`); `extract_job()` (a URL-only fallback) uses only universal Open-Graph/`<title>` signals, never a guessed vendor selector; `open_job`/`extract_job`/`detect_login` are identical across every adapter so a shared `BrowserAdapterMixin` supplies them once, wired to Phase 47's `TabManager`/`SessionManager`; `prepare_application` always raises `FeatureUnavailableError` this phase — declared, not implemented. Workday's adapter is an honest `FeatureUnavailableError` stub, matching `AshbyFormFiller`'s precedent. 45 new tests (fixture-driven delegation reusing each wrapped source's own fixtures, registry/detection, capability-evidence, real-Chromium browser-hook tests). No CLI wiring, no form-filling, no login automation, no `Opportunity`/`ats_urls.py` change, no new dependency, no version bump | Accepted |
| [0067](0067-search-planner.md) | Phase 49, fourth phase of the v2 arc: a "Search Planner" deciding what to search before discovery runs. The audit found `agents/planner/` already exists and ADR-0007 already named it "the Planner boundary," with `Decide` (ranking *after* discovery) as its first delegated capability — ADR-0007's original much larger LangGraph/LLM-cost-cascade coordinator vision was never actually built anywhere in this codebase (verified: no `langgraph`/`langchain` import exists under `src/`). **Decision:** a second capability inside that same existing boundary, not a new top-level `planner/` package — `execution_plan.py`/`provider_selector.py`/`budget.py`/`planning_rules.py`/`planner.py`, flat alongside `decide.py`. No `keyword_expander.py` (would be a pass-through wrapper around Phase 46's already-real `generate_search_queries`) and no `strategy.py` (diversification folded into `build_execution_plan`'s own assembly loop: queries outer, priority-ordered providers inner, so no provider can consume the whole budget just by appearing first). `provider_selector.order_providers` finally consumes `JobPreferences.preferred_ats_providers`, documented in ADR-0064 as captured-but-unconsumed until now. Budget (search-volume target) is explicitly kept distinct from `max_applications_per_day` (an application-rate limit) — conflating them would misapply one concept to an unrelated decision. `SearchTask.max_retries` is declared, not enforced — nothing in this phase executes a plan at all (no executor exists yet; that's future work), verified by an AST-based purity test scanning for any I/O import or `async def` across all five new files (none exist). 33 new tests. No CLI wiring, no AI/LLM call, no change to `Decide`/`AdapterRegistry`/the execution-safety boundary, no new dependency, no version bump | Accepted |
| [0068](0068-resume-variant-engine.md) | Phase 50, fifth phase of the v2 arc: a "Resume Variant Engine" — cover letter generation + resume-variant storage, on top of the unmodified tailoring pipeline. Most of Phase 50's brief already exists (`ResumeTailoringPipeline.run()` already does generate→gate→ATS-score; `ats_scoring.extract_jd_keywords` already is skill/JD analysis); what's new is a cover letter and a stored variant. **Decision:** the truthfulness gate is deliberately *not* extended to freeform prose this phase — atomizing/verifying arbitrary generated sentences is a real, separate design problem. Instead `domain/cover_letter.py::assemble_cover_letter` is a **deterministic, zero-LLM** template that copies only `content.summary` and up to three already-gate-approved highlights verbatim into a letter shape — no new fabrication surface, so no new gate is needed. `domain/resume_variants.py::select_closest_variant` ranks previously-approved variants by keyword overlap (reusing `extract_jd_keywords` unchanged) but is purely advisory — `ResumeVariantEngine.build_materials()` (renamed from `.prepare()` in Phase 51 to avoid colliding with the release-invariant contract's literal `.prepare(` ban once the CLI actually called it) always calls the unmodified pipeline regardless of its answer, so it structurally cannot influence what gets gated. `SqliteResumeVariantStore` (added into the existing one-file `storage/sqlite.py` convention) is append-only, mirroring `SqliteApplicationStore`; `ResumeVariantEngine` itself has zero storage dependency (proven by an AST canary) — it returns a built-but-unsaved `ResumeVariant`, the same "pipeline doesn't touch storage either, cli.py does" shape already established. 24 new tests. No CLI wiring, no change to `ResumeTailoringPipeline`/`LLMResumeGenerator`/`LLMTruthfulnessGate`, no new dependency, no version bump | Accepted |
| [0069](0069-application-preparation-engine.md) | Phase 51, sixth phase of the v2 arc: an "Application Preparation Engine" that opens a real browser, fills a live application form, and stops before Submit. The audit found this is almost entirely not greenfield: `agents/apply/browser_applicator.py` (`BrowserApplicator`, unwired from the CLI, ADR-0020/0028/0032) already implements the exact sequence the brief describes — per-platform `FormFiller` fills identity/résumé fields, `question_answerer.py` (ADR-0031) classifies and auto-answers everything else it safely can, unresolved fields are manifested for a human — and only then clicks submit. **Decision:** extract the field-detection/triage helpers (`_required_unknown_elements`/`_field_question_text`/`_try_fill_boolean_select`/`_triage_unhandled_fields`/`_fields_still_empty`) out of `browser_applicator.py` into a new shared `agents/apply/field_inspection.py` (verbatim, no behavior change — proven by browser_applicator's own unchanged 32-test suite passing before and after), and build a new `agents/application/engine.py::ApplicationPreparationEngine` that composes them plus Phase 47's `BrowserManager`/`SessionManager`/`TabManager` — reaching the identical point `BrowserApplicator.submit()` reaches right before `page.click(filler.submit_selector)`, and then simply returning. No submit-selector/click reference exists anywhere in the new module at all, proven by an AST-based source-scan test, the same structural-guarantee discipline `SessionManager`'s login-safety test already established. No new `field_detector.py`/`answer_engine.py` (would duplicate the extraction/`question_answerer.py`); no `field_mapper.py` (most of the brief's example fields — website/LinkedIn/GitHub — have no `MasterProfile` field to map from at all, so they honestly fall through to the manifest); no `upload_manager.py` (Lever's real upload already lives in `LeverFormFiller`, unchanged; cover-letter upload is attempted nowhere since no platform has a verified selector, `AdapterCapabilities.supports_cover_letter_upload` being `False` for every provider). `domain/application_session.py::ApplicationSession` (pure data, no submission-confirmation field can exist on it at all) is the new result type, following Phase 50's precedent of putting pure results in `domain/` rather than the brief's suggested `agents/application/session.py`. Found and fixed a real collision along the way: `ResumeVariantEngine.prepare()` (Phase 50) would have tripped the existing release-invariant test's literal `.prepare(` ban the moment `cli.py` actually called it — renamed to `.build_materials()`. New `career-agent prepare` CLI command and `SqliteApplicationSessionStore`. 19 new tests (911 total). No submit click anywhere, no change to `BrowserApplicator`/`TieredApplicator`/the execution-safety boundary, no new dependency, no version bump | Accepted |
| [0070](0070-human-review-center.md) | Phase 52, seventh phase of the v2 arc: a Human Review Center — the sole `READY_FOR_REVIEW` → `APPROVED` transition boundary. `ApplicationSession` already carries everything a human needs to decide (Phase 51); this phase adds the decision itself. **Decision:** `domain/review.py::ReviewSession` references `application_session_id` plus a few cheap denormalized display fields rather than duplicating warnings/missing-fields/filled-fields/uploaded-files/résumé-variant/cover-letter content — the same "denormalize identity fields, not full content" precedent `SqliteApplicationStore`'s own `company`/`title` columns already set, proven structurally by a test asserting those fields don't exist on `ReviewSession` at all. `format_review_summary` is pure, deterministic text formatting (no AI, no filtering — every warning and missing field always shown) and lives in `domain/` alongside `ReviewResult`, not split into separate `agents/review/review_summary.py`/`review_result.py` files as the brief suggested (no capability behind the split). `agents/review/review_engine.py::ReviewEngine` has **zero** browser dependency — proven by two AST-based source-scan tests (no `integrations.browser` import, no `.click(` call anywhere), the identical structural-guarantee discipline `ApplicationPreparationEngine`'s own no-click test already established. Only an explicit "y"/"yes" answer produces `APPROVED` (reusing `confirm_submission`'s no-default-to-yes discipline); `CANCELLED`/`TIMEOUT` are reachable via an injectable-exception seam on `input_fn`, since a portable stdin timeout is a real, separate, `SIGALRM`-is-POSIX-only problem this phase doesn't need to solve to prove the states exist and are handled correctly. `career-agent prepare` now also writes a JSON session-file handoff (mirroring `discover`'s own opportunity-file-handoff convention exactly) that the new `career-agent review --session <path>` command consumes. No new `review_storage.py` either — `SqliteReviewSessionStore` joins the existing one-file `storage/sqlite.py` convention. Checked the new code against Phase 51's found `.prepare(`-collision release-invariant test explicitly; no second collision. 36 new tests (11 pure domain, 11 engine, 7 storage, 7 real end-to-end CLI — `run_review_command` is fully offline so it's tested directly, not just for file-loading). 947 total. No Submit, no browser mutation, no AI review, no résumé/field editing, no Submission Engine, no new dependency, no version bump | Accepted |
| [0071](0071-human-approved-submission-engine.md) | Phase 53, the boundary every phase since Phase 24 (ADR-0050) built toward and refused to cross — undertaken only after the user's own explicit, detailed, safety-first authorization. The audit found the actual executor already exists: `BrowserApplicator` (Tier 2, ADR-0020/0028/0032) already does fill → triage/auto-answer → click submit → check for a challenge → return `ApplicationSubmitted`, unwired specifically pending this boundary. **Decision:** build the fail-closed gate in front of it, not a second implementation. `agents/submission/submission_engine.py::SubmissionEngine` checks, in order: the review and application session actually pair together; the review is `APPROVED`; the application session is still `READY_FOR_REVIEW`; the résumé about to be submitted matches (content-for-content, via a new `SqliteResumeVariantStore.get`) what was actually reviewed; `domain/execution.py`'s unmodified fail-closed boundary (`execute_allowed`/`resolve_source_policy` — only Greenhouse/Lever/Ashby resolve `ASSISTED`) evaluated as a dry run *before* ever asking the human anything; and only then a real 5-second countdown plus a blocking ENTER confirmation, un-bypassable, Ctrl+C recorded as `CANCELLED`. No fabricated verification — no platform's "Thank you" page/confirmation number has ever been verified in this codebase, so `confirmation_id`/`confirmation_url` stay `None` with an explicit warning; the only verified signal is `BrowserApplicator`'s own event distinction, reused unchanged. A generic exception during the actual submit call (which contains the click) is recorded `UNKNOWN`, never `FAILED` — the same "ambiguous evidence can never become a definite result" rule `domain.execution.outcome_from_ack` already enforces. No new `verification.py`/`tracker.py`: verification *is* the reused event distinction, `SqliteSubmissionResultStore` joins the existing one-file `storage/sqlite.py` convention, `storage/excel.py` gains a small `export_submissions()` sibling. **Found and deliberately rewrote** (not weakened) `tests/test_phase28_release_invariants.py`'s blanket `.submit(`/`.prepare(` ban — correctly tripped by this phase's own `engine.submit(` call — into a stronger, more precise invariant proving `execute_allowed()` genuinely runs before the real executor call. 27 new tests; 974 total. No CAPTCHA/MFA automation, no password storage, no silent retry, no AI verification, no change to `BrowserApplicator`/`ApplicationPreparationEngine`/`ReviewEngine`, no new dependency, no version bump | Accepted |
| [0072](0072-web-dashboard-read-api.md) | Phase 54, the first step toward a web dashboard on top of the now-complete backend core workflow — deliberately scoped down from the full brief (new frontend toolchain + FastAPI layer in one pass) to the backend API surface only, and further scoped to **read-only**. `src/career_agent/api/` is a thin FastAPI layer: each router (`applications`/`reviews`/`submissions`/`resume_variants`) wraps exactly one existing store's `all_*()` method; `analytics.py` adds one `collections.Counter` aggregation step (no new metrics engine, deliberately not touching the older `SqliteApplicationStore`/funnel pipeline — a documented separate pipeline since Phase 51); `settings.py` redacts every API-key/token field to a `configured: bool` flag, never its value. **No route can trigger discovery, tailoring, review approval, or submission** — `SubmissionEngine` is not imported anywhere under `career_agent.api`, enforced structurally (not just documented): `CORSMiddleware` only allows `GET`, and a dedicated test enumerates every route the app actually registers and asserts each one's methods are a subset of `{GET, HEAD, OPTIONS}`. New CLI subcommand `career-agent serve [--host] [--port]` runs `uvicorn.run(create_app())`; `fastapi`/`uvicorn` are a new optional `web` extra, imported lazily inside `run_serve_command` so every other command keeps working with a plain install. 15 new tests; 990 total. No authentication (single-user, localhost-only; multi-user auth is a later phase); no React frontend yet — the immediate follow-up phase, building against this now-tested API contract | Accepted |
| [0073](0073-react-dashboard-frontend.md) | Phase 55, the React frontend ADR-0072 named as its own immediate follow-up. `frontend/` is a React 19 + TypeScript + Vite app (TailwindCSS + hand-written shadcn-style primitives, TanStack Query, React Router, React Hook Form, Recharts, Lucide) with 8 pages, a responsive sidebar/navbar layout, and persisted dark mode. **Every page renders real data from the six existing `GET` routes only** — `services/api.ts` mirrors `api/routers/*.py` exactly; cross-route joins are pure functions over already-fetched responses (`lib/derive.ts`), the same "aggregation is presentation logic" precedent `analytics.py` already set server-side. The brief's write actions (search, approve/reject, submit) have no backing endpoint — rather than fabricate them, `components/CliOnlyAction.tsx` renders a disabled button naming the exact real CLI command instead, with each page explaining why (Submission Queue: no live browser state/countdown can be shown, since ADR-0071's terminal confirmation flow can't be safely reproduced over HTTP). UI primitives are hand-written shadcn-style (no network path to the shadcn registry in this sandbox). New CI job `verify-frontend` (matrix ubuntu/windows) runs npm ci/type-check/lint/vitest/build on every push/PR. 15 new frontend tests; manually verified against a real running `career-agent serve` with seeded data, screenshotted in light/dark themes and at a mobile viewport. Zero backend files changed; full backend suite re-confirmed unmodified (990 passed) | Accepted |
